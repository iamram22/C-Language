#include <stdio.h>

int main() {
    int r = 6;
    int height = 8;
    printf("The area of circle with radious %d, is %f\n", r, 3.14*r*r);
    printf("volume of the cylinder with radius %d, and height %d is %f", r, height, 3.14*r*r*height);
    return 0;
}