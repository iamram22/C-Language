#include <stdio.h>

int main()
{
    // Explian step by step evaluation of the following
    int x = 2, y = 3, z = 3, k = 1;
    float e = 3 * x / y - z + k;
    // 3*2=6
    // 6/3=2
    // 2-3=-1
    //-1+1=0
    // final output 0;
    printf("The value of 2 is %f\n", e);

    float a = 3.0 + 1;
    printf("the vallue of a is %f\n", a);
    return 0;
}